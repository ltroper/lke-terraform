# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    cd infra
    touch terraform.tfvars
    echo 'token = "adc2c48133c5e92019596483505056b9068970c708424885196af11a56223fda"
          pass = "kqiuw4hrgfcb987"
          k8s_version = "1.26"
          label = "lke-tf"
          region = "us-east"
          pools = [
            {
              type : "g6-standard-2"
              count : 3
            }
          ]' > terraform.tfvars
    terraform init
    terraform plan
    terraform apply --autoapprove


  displayName: 'Run a one-line script'


